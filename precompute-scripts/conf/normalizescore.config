params {
   masks_processing_tags = ''
   targets_processing_tags = ''

   // Normalize scores params
   normalizescore_concurrency = 0
   normalize_score_processing_tag = ''
   normalize_score_batch_size = 10000
}
